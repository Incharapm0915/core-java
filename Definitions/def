Java
High level Object Oriented Programming Language

Object Oriented
Approach to build any Software / Write any Program

C is a Procedural Oriented Programming Language

Programming language
The language which is writen on computer

Program
Group of Activities/Events combined together to perform a specific task

Programming language has purpose of providing Syntax and Semantics

Program will be in Static
Execution Stage is a Process
One who Executes is a Processor

String is a class in java / Object Oriented reference type
All Keywords are in lowercase

Variables name is also called as References. Reference = "Pragya" , external References = userName

 System.out.println("My Name is: " + userName);
                     String Reference in java
Or else it is a Internal reference only this = " My Name is:"

Datatype = help to represent the type of value stored on a reference
Types
1)Primitive
2)Object Reference type = String,arrays,interface,enum,class etc

variableName/reference(external/explicit)

Variable = Registered area allocated on a java memory.

4 types of variable
1)Local -  Always Inside Method
2)Static - Outside method
3)Instance/non static
4)Parameter

low level = any language which directly interact with the hardware.

high level= any language which  do not directly interact with the hardware.

source code(.c) -compiler- assembler -processor ( amd or intel )
assembler out is binary format
every processor has therir own assembler whose purpose is to write the instruction
platform dependency= the combination of os and processor is called platform dependency
source code(.java) -java compiler(java c) - byte code(.class) -JVM (java) - executed with n
byte code is platorm independent which is generated by java compiler
platform independency -
jdk1.8 download - 8u261


16/02/20206
local and static with same variable names will be in different memory location hence they are considered to be different itself

Java Compiler converts source code to byte code, Then the program will be run using Java ClassName which is done by JVM
metaspace - Class Information is stored(UserProfile) - metadata of a class
static variable - userName
main()

Stack - Class in JAVA / Also a Datatype / Purpose is to execute a method

JVM will create a frame for the execution
In that frame firstly name of the Method(main) is stored
(String[] args) - Parameter - stored in stack
Local Variables also stored in stack inside frame
Object reference also stored in stack inside frame
Current line information also stored in stack inside frame
Next line information also stored in stack inside frame
Return type Info also stored in stack inside frame

The value will be stored in Constantpool(to store values)

Preference will be for local so if we declare 2 times with same variable names it will not give error as one will be stored in 
metaspace - the static one 
in stack - local one
JAVA is a Case Sensitive
Static and Non static will be initially stored in metaspace with class and we cannot declare the same variable name twice
main initially get stored in metaspace later it will be in stack

Null - Default - given by compiler
All these are default values
String - null

int - 0(byte,shot)
char - space
boolean - false
float - 0.0
double - 0.0


class UserProfilee(object reference datatype) {

    publicstatic(assess specifier) void(keyword/return type) main(String(object reference datatype)[] profile(reference)) {
        int(datatype) age=90;
         age=78;(re initialization)
        System(object reference datatype).out.println(method)("The user's age is: " + age);
    }
}   
public static void - keywords
public static - access specifier

Address[]- this is known as custom datatype

Array initialization using new is called as literals

Array - Collection of references of same type

JVM is the caller of main(invoke)


to write own method syntax
method
<access-specifier> returnType methodName(){
//statements
}

Array: collection of reference of same type

syntax: Datatype arrayRef[]={ref1,ref2};

use of array: hold more than references of same type

where: inside class,inside method,inside blocks

<access-specifier> returntype methodName(parameter)
Method header                  method signature

caller of main is JVM
caller of method is main

Method:
group of java statements which is used to perform operation on references/variables/field

use of method - code reusability
Main will not return any value its just executing the content

println is a method

